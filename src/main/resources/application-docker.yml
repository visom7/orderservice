# Docker configuration
service:
  url: http://host.docker.internal:8091
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/orderservice?useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 1234

server:
  port: 8090